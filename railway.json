{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -m jobs.daily_run --skip-fetch --dry-run",
    "restartPolicyType": "never"
  }
}